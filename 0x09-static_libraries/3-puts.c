#include "main.h"
#include <stdio.h>

/**
 *_puts - prints a string, followed by a new line, to stdout.
 *@str: take the input from another
 * Return: void
 */
void _puts(char *str)
{
	printf("%s\n", str);

}
